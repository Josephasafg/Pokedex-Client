import React from "react";
import {List, Typography} from '@mui/material';
import classes from "../PokemonCard/PokemonCard.module.scss";

export const StatsList: React.FC = () => {
    return (
       <div></div>
    )
}